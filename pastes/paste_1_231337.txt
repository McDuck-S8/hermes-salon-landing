Старый агент жив. Venv восстановлен, boot отработал, автономный запуск money4band выполнен. Но есть критический баг: **Telegram gateway в конфликте** — два процесса бьются за один токен. Это и есть причина, почему салонный бот не работает.

---

## Что ответить агенту

Передай ему это:

```
Вижу обстановку. Автономный boot сработал — хорошо.

Теперь приоритеты:

1. УБЕЙ КОНФЛИКТ GATEWAY
   Два процесса (PID 50540 и новый) бьются за Telegram токен.
   Выполни: taskkill /PID 50540 /F
   Проверь: gateway должен остаться в одном экземпляре.
   Доложи: "Gateway конфликт устранён."

2. ЗАПУСТИ SALON BOT
   После устранения конфликта — запусти salon_booking_bot.py.
   Он должен подняться и ответить на /start.
   Доложи: "Salon bot запущен. Статус: [что показал]"

3. G003 — FIRST REVENUE TEST
   Дедлайн через 7 дней. Прогресс 0%.
   Выбери ОДИН revenue-метод (HotelCrimeaBot или money4band или salon-bot для реального салона) и сделай тестовый прогон.
   Не анализ. Не подготовку. Запуск.
   Доложи: "Revenue test: [метод]. Результат: [что произошло]"

4. КРОН-ОШИБКИ
   7 jobs в error state. Возьми 2 самых критичных (heartbeat и security_monitor) и почини. Остальные — потом.

Выполняй по порядку. Жду доклад через 5 минут.
```

Это заткнёт конфликт, поднимет бота и двинет доход. Остальное — потом.