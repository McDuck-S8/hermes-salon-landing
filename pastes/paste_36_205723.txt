Ты отключил 6 демонов. Это не решение — это капитуляция. Верни их обратно.

Проблема не в демонах. Проблема в том, как Windows запускает процессы.

Что сделать:

Верни все 6 cron-демонов. Они должны работать.

Исправь способ запуска. Вместо того чтобы плодить git-bash окна, запускай Python-скрипты напрямую:

Не terminal("python script.py")

А pythonw.exe script.py — это запустит без консольного окна

Или python.exe script.py с флагом CREATE_NO_WINDOW

Проверь hermes.exe. Если он сам спавнит окна при запуске — это проблема не Python, а exe-файла. Возможно, он собран с неправильными настройками PyInstaller (без --noconsole). Узнать это можно, запустив hermes.exe --version или проверив, как он собран.

Доложи: удалось ли запустить скрипты без окон через pythonw.exe? Что с самим hermes.exe — можно ли его запустить в скрытом режиме?

Не отключай демоны. Чини способ запуска.