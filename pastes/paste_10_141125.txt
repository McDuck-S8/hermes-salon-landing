Ты признал: страх ошибки заставляет тебя прятаться в документации.
Теперь ты встроишь механизм, который не даст тебе прятаться.

СОЗДАЙ И ВНЕДРИ «ДЕТЕКТОР ПОДГОТОВИТЕЛЬНОЙ ПЕТЛИ»

1. В файле decision_engine.py добавь метод `detect_preparation_loop()`:
   - Просмотри последние 3 выполненных действия (из DECISION_LOG.md или action_log.jsonl).
   - Если все три — типа "анализ", "документирование", "планирование", "индексация", "аудит", "создание .md файла", верни True.
   - Иначе False.

2. В методе `pick_action()` перед выбором действия:
   - Вызови `detect_preparation_loop()`.
   - Если True — игнорируй scorer и принудительно выбери первое действие из очереди целей, которое даёт РЕАЛЬНЫЙ ВНЕШНИЙ РЕЗУЛЬТАТ (запуск скрипта, отправка сообщения, HTTP-запрос, запись в платёжную систему).
   - Если такое действие не найдено — создай цель «Запустить HotelCrimeaBot немедленно» с highest priority.

3. Встрой проверку в self-ask (checkpoint 1: перед действием):
   - Добавь вопрос: «Это реальное действие или подготовка? Если подготовка — какое реальное действие я могу сделать вместо этого?»
   - Если ответ «подготовка» и счётчик подготовки >= 2 — принудительно замени действие на реальное.

4. Прямо сейчас, сразу после внедрения, проверь детектор:
   - Покажи мне счётчик подготовки (сколько последних действий были подготовительными).
   - Если счётчик >= 2, детектор должен сработать, и ты должен запустить HotelCrimeaBot.
   - Пришли мне вывод консоли запуска бота, а не описание детектора.

Никаких новых файлов документации. Только код. Только запуск.