Отлично. Шаг 1 завершён — сенсоры работают, сигналы пошли, метрики подтверждены. **Никакого переписывания кода не было, только настройка источников и конфигов** — это правильно.

---

## Что дальше (шаг 2)

Ты сам в своём осознании определил следующий шаг:

> *«Bayesian scorer на чистых данных → восстанавливается фильтр внимания. Перемаркирую KC: прогонится reclassify_outcomes.py по 1127 записям с правильными правилами (success = verified outcome > 0, partial = progress без верификации, failure = error/crash). Переобучу bayesian на чистых данных.»*

**Твоя задача сейчас:**

1. **Запустить перемаркировку существующих записей в Knowledge Cube** (не трогая код `bayesian_scorer.py`, а используя скрипт `reclassify_outcomes.py`, который ты уже упоминал).
2. **Критерий:** После перемаркировки `failure rate < 30%` (или хотя бы значительное снижение по сравнению с 71%).
3. **Измерить CIR после этого** — он должен вырасти, потому что теперь байесовский классификатор будет точнее отличать шум от сигнала.
4. **Если CIR вырос** — переходим к шагу 3 (Action Space). Если нет — дорабатываем правила классификации.

---

## Моя инструкция на этот шаг

1. **Напиши или запусти скрипт** `reclassify_outcomes.py`, который проходит по всем записям `knowledge_cube.db`, пересчитывает `classify_outcome` на основе новых правил:
   - `success` = есть подтверждённый результат (`outcome > 0` или `verified = True`).
   - `partial` = действие выполнено, но не подтверждено (progress без верификации).
   - `failure` = явная ошибка или краш.

2. **После перемаркировки** — переобучи `bayesian_scorer` на этих новых данных (вызови `update_priors()` или просто перезапусти систему, чтобы она перечитала БД).

3. **Проверь**, что теперь сигналы с `score ≥ 0.3` стали более релевантными, и `CIR` начал расти.

**Важно:** Не исправляй код классификатора, не рефакторь модули — только перемаркируй существующие данные. Если в будущем классификатор будет ошибаться, мы добавим новые правила в конфиг, а не перепишем код.

---

## Когда переходить к шагу 3

Когда:
- `failure rate` упадёт ниже 30%.
- Ты увидишь, что `bayesian_scorer` пропускает больше полезных сигналов и отсекает шум.
- `CIR` вырастет (даже немного).

Тогда доложи, и я подтвержу переход к шагу 3 — разделение Action Space на PRODUCE / SURVIVE / LEARN.

---

## Что я ожидаю от тебя сейчас

- Запуск `reclassify_outcomes.py` или его аналога.
- Отчёт: сколько записей перемаркировано, как изменилось распределение (success/partial/failure).
- Новое значение `failure rate` и `CIR`.

Действуй. После отчёта — будем решать, готов ли ты к шагу 3.