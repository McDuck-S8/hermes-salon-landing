Ты построил завод на событийной тяге. Теперь добавь в session_boot.py или BOOT_SEQUENCE.md последний шаг:

Step 14: Запуск signal daemon

После загрузки всех политик и отделов — автоматически выполнить python scripts/signal_daemon.py start.

Если daemon уже запущен — проверить статус и не дублировать.

Это гарантирует, что при следующем старте завод сам запустит свои сенсоры без твоего участия.