«bootstrap.py — это инициализатор, а не хук для каждого ответа. Он должен запускаться:

При старте системы (через Task Scheduler).

Если event_beat("system_ready") не поступал более 24 часов.

По команде «сделай bootstrap» (вручную).
После каждого сообщения — НЕ ЗАПУСКАТЬ. Если система уже в состоянии READY — никаких дополнительных bootstrap’ов.
Исправь это сейчас. Сделай проверку: перед запуском проверяй файл cache/system_ready.flag. Если он есть и ему меньше 24 часов — не запускай.