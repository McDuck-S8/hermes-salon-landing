---
name: 2026-07-14-00-17-33
description: "Auto-generated from 2026-07-14_00-17-33.md"
trigger: "When user asks about 2026-07-14_00-17-33 concepts"
usage: 2026-07-14-00-17-33
Revisit: 2026-07-31
---

# Cron Job: backlog-reminder

**Job ID:** 812dee47a965
**Run Time:** 2026-07-14 00:17:33
**Schedule:** 0 */4 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

You are a backlog worker running every 4 hours. Do the following:
1. Read the task backlog from http://localhost:8766/data/tasks.json
2. Find all tasks with status "ready"
3. For the highest priority ready task, attempt to complete it:
   - If it's a documentation task (crystal, AGENTS.md), read the relevant source files and write/update the markdown docs
   - If it's a research task (upstream, trends), use web_search and web_extract to gather info
   - If it's a code task, read relevant files and fix/implement
4. After completing: update the task status to "done" via POST to http://localhost:8766/api/task/move with JSON {"task_id":"ID","new_status":"done"}
5. Write a summary to D:/Portable_Soft/hermes/dashboard_data/data/agent_log.md with format: "[2026-07-12 20:00] Completed TASK_ID - TASK_NAME - what was done"

Important: always produce a real result. If you can't complete a task fully, document what's blocked and why. Never fabricate results.

## Response

## Summary

Completed task **t_affiliate_bot** — Built affiliate bot adapted from Tara Bot for the Russian market with CPA networks integration.

### What was created at `D:/Portable_Soft/hermes/tara-bot-ru/`:

**Core Application:**
- `src/bot.py` — Telegram bot entry point with `/start`, `/reset`, `/uptime` commands
- `src/agents.py` — LLM agent with tool-calling loop, prompt caching, adaptive thinking (Claude/OpenAI)
- `src/config.py` — Environment config with all CPA network API keys
- `src/tools/serpapi.py` — Flight search (Google Flights) + Product search (Google Shopping) with RU city mappings, RUB currency
- `src/tools/cpa_links.py` — **NEW**: MyLead, Admitad, KMA.biz integration with offer search and affiliate link generation
- `src/tools/__init__.py` — Unified exports

**Deployment:**
- `Dockerfile` — Python 3.11 slim container
- `fly.toml` — Fly.io config (Frankfurt region, 1GB RAM, shared CPU)
- `DEPLOY.md` — Complete deployment guide with API key sources
- `.github/workflows/monitor.yml` — Daily price monitor cron (9AM MSK via GitHub Actions)
- `scripts/monitor.py` — Checks 6 popular RU routes daily, sends Telegram alerts

**Documentation:**
- `README.md` — Full feature list, tech stack, deploy instructions, cost breakdown (~$0.50/month)
- `CHANGELOG.md` — v1.0.0 release notes
- `.env.example` — Template with all required/optional env vars

### Key Adaptations for RU Market:
- Russian city IATA codes (MOW, LED, AER, OVB, SVX, KZN, VVO, KHV, etc.)
- CIS airports (ALA, TSE, KBP, MSQ, TBS, EVN)
- Currency: RUB instead of VND
- Language: Russian system prompt and responses
- CPA networks: MyLead (crypto/nutra/gambling), Admitad (Ozon/WB/AliExpress/Booking), KMA.biz (crypto/betting)

### Usage:
```bash
cp .env.example .env  # fill in API keys
chmod +x run_bot.sh
./run_bot.sh
```

Or deploy to Fly.io:
```bash
fly launch --no-deploy
fly secrets set TELEGRAM_TOKEN=xxx ALLOWED_USER_ID=xxx SERPAPI_KEY=xxx ANTHROPIC_API_KEY=xxx MYLEAD_API_KEY=xxx ADMITAD_API_KEY=xxx KMA_API_KEY=xxx
fly deploy
```

The task status update API returned "invalid id or status" but the work is complete and logged to `dashboard_data/data/agent_log.md`.
