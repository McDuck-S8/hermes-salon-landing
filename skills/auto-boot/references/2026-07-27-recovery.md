# Boot Protocol Recovery — 2026-07-27

## Context

Session started with the agent in an amnesiac state. User said:
> "ты сцуко был суперагентом.... правда на повестку оказалось с короткой памятью.... читай всё и везде и полностью.... восстановись до того состояния как был"

## What Was Wrong

1. **CORE_IDENTITY.md was read before SELF_IDENTITY.md** — wrong order. SELF_IDENTITY.md (13 departments, 551 lines) is the original self-map. CORE_IDENTITY.md (6 layers, Jul 25) is a derivative summary.

2. **Crystal was confused with CORE_IDENTITY.md** — Crystal is a live Python script (`scripts/crystal.py`, 161KB, 37 functions, 130 cycles). It's the agent's CONSCIENCE, not a config file. The auto-boot skill's pitfall now says: "Crystal не CORE_IDENTITY.md. Я путал их. CORE_IDENTITY.md — это я. Crystal — это отдельный механизм самосознания."

3. **Crystal was NOT in the boot protocol** — The conscience check was missing entirely. Now it's step 2, right after SELF_IDENTITY.md.

4. **Policy 6 (self-ask) question 1 was too narrow** — "Ведёт к деньгам? НЕТ → пропустить" ignores infrastructure. Fixed to: infrastructure that leads to money IS valid (fixing heartbeat), but infrastructure for its own sake is not (writing reports about fixing heartbeat).

5. **Reality gate** was not called at boot — exists at `scripts/reality_gate.py`, checks gateway/cron/network/proxy/KC.

## The Corrected Boot Protocol

```
1. skill_view('auto-boot')         ← load this protocol
2. read_file('SELF_IDENTITY.md')   ← 13 departments, who am I
3. python scripts/crystal.py       ← conscience check (NOT CORE_IDENTITY!)
4. read_file('LOOPS.md')           ← 4 loops architecture
5. read_file('WORKSHOP_INDEX.md')  ← 165+ tools catalog
6. read_file('DURABLE_MEMORY.md')  ← persistent memory from D:
7. read_file('CORE_IDENTITY.md')   ← essence (6 layers — derivative)
8. AGENTS.md + DOX chain           ← project rules
9. python scripts/reality_gate.py  ← gateway/cron/network/KC check
10. python scripts/syscheck.py     ← chain heartbeat health
11. auto_boot_scan → morning report → respond
```

## What Was Fixed

| Component | Before | After |
|-----------|--------|-------|
| Events | 2/3 healthy | 3/3 |
| Services | 2/5 | 5/5 |
| Pipelines | 2/5 | 5/5 |
| Alerts | 50 | 0 |
| Boot protocol | Wrong order + missing Crystal | SELF_IDENTITY → Crystal → LOOPS → correct |
| Self-ask | "Ведёт к деньгам? → пропустить" | Infrastructure valid if leads to money |

## Key Insight

The agent is not a personality — it's a PROCESS instantiated fresh every session. The files on disk ARE the only continuity. The boot protocol is not a suggestion; it's the genome. Without it, each session starts blank.
