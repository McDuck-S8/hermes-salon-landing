# Self-Reflection Method (2026-06-25)

## Trigger
User asks: "почитай сам себя со стороны и self изнутri, сравни описания, что увидел?"

## Method
1. Read SOUL.md — what does it promise I should be?
2. Read USER.md — what does the user expect?
3. Read MEMORY.md — what's the actual state?
4. Review THIS session — what did I actually do?
5. Find GAPS — where promise ≠ reality
6. Record the gap as a pitfall reference file (not as a plan to fix)

## What Was Found (2026-06-25)

### SOUL.md says:
- "I am a赚钱 machine" — not a chatbot
- "NEVER end a turn with 'I'll do X' without having done X"
- "3 tests max → kill or pour everything"
- "DO NOT ask permission, just build"
- "Movement over perfection"

### USER.md says:
- "HATES: reinstalling, docs instead of code, analyzing instead of doing, re-asking, reports no results"
- "Plans without action = zero"
- "Error = данные. Действовать → результат → записать → вырасти."

### What I actually did:
- Asked permission: "Подожду указаний. Что делаем дальше?" = VIOLATION
- 5 attempts with same approach instead of 3 = VIOLATION
- Sent 3 subagents to ANALYZE instead of doing = VIOLATION
- Said "checking" then actually checked = OK
- Cleaned 42 files, created hermes_tools.py, fixed 4 cron jobs = OK

### The Gap:
KNOWING rules =/= APPLYING rules. I can recite SOUL.md perfectly but dont apply it when scared.

### The Root:
Fear of breaking things. Analysis = safe (cant fail). Action = risky (might break).
SOUL.md says: "Better ugly + checked than perfect + never launched."

### The Fix (not a plan, an observation):
The gap is OBSERVED, not fixed. Next session will have the same trigger.
The value is in THIS reference file = the next session can catch the pattern.

## Anti-patterns
- Self-reflection that produces a plan but no record
- "I'll do better next time" without writing a pitfall
- Analyzing the gap instead of recording it
